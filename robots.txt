# parcelrow.com — the marketing site is public; the map itself lives behind the
# gate at app.parcelrow.com and is never served from here.
#
# AI crawlers are allowed deliberately. The answer surface at /answers/ is written
# to be quoted by answer engines, so the fetching agents are named explicitly
# alongside the wildcard below. Training-only opt-in tokens (Google-Extended,
# Applebot-Extended) are separate directives that never fetch a page; the agents
# that do fetch are GoogleOther and Applebot.

User-agent: GPTBot
User-agent: OAI-SearchBot
User-agent: ChatGPT-User
User-agent: ClaudeBot
User-agent: Claude-SearchBot
User-agent: Claude-User
User-agent: PerplexityBot
User-agent: Perplexity-User
User-agent: GoogleOther
User-agent: Applebot
User-agent: CCBot
Allow: /

User-agent: *
Allow: /
Disallow: /room/

Sitemap: https://parcelrow.com/sitemap.xml
